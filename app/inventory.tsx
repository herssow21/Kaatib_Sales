import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  actionsCell: {
    flex: 0.3,
    flexDirection: "row",
    justifyContent: "flex-end",
    alignItems: "center",
    gap: 8,
  },
});
